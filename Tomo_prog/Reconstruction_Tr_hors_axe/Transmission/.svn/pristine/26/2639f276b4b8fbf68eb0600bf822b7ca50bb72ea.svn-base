#include <iostream>
using namespace std;

#include "cl2.h"


void 
cl2::do2()
{
  cout << "bonjour de cl2";

  double *a1, *a2;
  ARRAY_ALLOC(a1, x * y, double);

  FFT -> import_from(a1, a1, false);
  FFT -> set_fourier_forward();
  FFT -> export_to(a1, a1, false);


}
